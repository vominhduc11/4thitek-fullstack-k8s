apiVersion: v1
kind: Service
metadata:
  name: fe-dealer-service
  labels:
    app: fe-dealer
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  selector:
    app: fe-dealer
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: fe-dealer-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: dealer.4thitek.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: fe-dealer-service
            port:
              number: 80